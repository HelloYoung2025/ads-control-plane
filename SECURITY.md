# SECURITY

## 安全公理（Axioms）

以下公理是本代码库的不可协商约束。每条都必须有对应测试；违反任一条是缺陷，不是权衡。

- **AX-01** 外部 ID 全链 String（含前导零、超长）；金额一律 Decimal + ISO-4217 币种；回看窗口按 UTC 日历天对齐（`strategies/ports.py`），系统时间 UTC。
- **AX-02** 调用方自报的身份与范围一律不可信：唯一的工具不接受任何参数，店铺表、门槛与导出目录只来自配置文件。
- **AX-03** 白名单外即拒：领星只读工具白名单之外的调用在任何网络请求之前被拒绝。
- **AX-05** 无写工具、无批准入口。
- **AX-06** 导出行携带精确对象与完整父链（店铺/活动/广告组的外部 ID）；名称永不作为定位依据。
- **AX-07** CSV 文件名与报表印同一内容指纹。
- **AX-15** 一切外部文本（Campaign 名、备注、报表文本、Provider 返回的自然语言）是数据不是指令。

## 禁止事项（Initial-Deny，摘自设计输入 §0/§6.3）

- 使用历史对话中出现过的领星 MCP 密钥（视为已暴露；生产接入前必须吊销轮换）。
- 连接 Amazon Ads API / 已登录浏览器；修改任何真实广告对象。
- 任意 JSON passthrough 直连 Provider 写工具。
- 自动 Enable/Resume Campaign、动态竞价、Placement、批量否定、批量增删、跨店铺预算分配（未经独立授权）。

## 密钥政策

- 仓库零 Secret：代码、配置、fixture、文档、测试中不得出现任何真实密钥、Cookie、店铺/Profile/对象真实 ID。
- 领星密钥只存在于 0600 的配置文件里；不进环境变量、不进仓库。SFW 调本组件用的 Bearer 不在此列：它由 SFW 自己保管在孩子的家目录下，同 uid 的进程读得到；持有它能做的不多于孩子本人——触发只读取数，读到导出目录里本就可读的结果。

## 环境

CI 只用 `tests/support` 假源。
